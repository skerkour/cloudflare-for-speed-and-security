{{#> base }}
    <div class="flex flex-col">
        <div class="flex">
            <h2 class="text-3xl font-bold">
                {{ page.title }}
            </h2>
        </div>

        <div class="flex mt-10">
            <div class="prose prose-sm w-full h-full max-w-none">
                {{{ page.content_html }}}
            </div>
        </div>
    </div>
{{/base}}
