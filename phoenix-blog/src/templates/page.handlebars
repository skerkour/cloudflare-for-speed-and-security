{{#> base }}
    <div class="flex flex-col">
        <div class="flex">
            <h2 class="text-3xl font-bold">
                {{ page.title }}
            </h2>
        </div>

        <div class="flex">
            {{{ page.content_html }}}
        </div>
    </div>
{{/base}}
